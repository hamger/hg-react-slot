import PropTypes from 'prop-types';

const ToSlot = () => null;

ToSlot.propTypes = { slot: PropTypes.string };
ToSlot.defaultTypes = { slot: 'default' };
ToSlot.displayName = 'ToSlot';

export default ToSlot;
